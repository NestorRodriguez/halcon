import { RendererFactory2 } from '@angular/core';
export declare class MwHtmlService {
    private document;
    private renderer;
    constructor(rendererFactory: RendererFactory2, document: any);
    updateTags(selector: string, attributeName: string, attributeValue: string): void;
}
